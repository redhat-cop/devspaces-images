# This is an image descriptor for CEkit

schema_version: 1

name: &name "cekit-builder"
version: &version "1.0"
description: "Developer image for building and testing images using CEkit"

modules:
  install:
  - name: cekit